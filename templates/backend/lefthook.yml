pre-commit:
  parallel: true
  jobs:
    - name: lint
      run: pnpm lint --fix-staged
      stage_fixed: true

    - name: types
      glob: "*.{ts,tsx,json}"
      run: pnpm test:types

pre-push:
  jobs:
    - name: test
      run: pnpm test
